.DS_Store
.env
.vscode/
node_modules/
yarn-error.log

# Rollup output; regenerated by `yarn build` in every CI workflow.
# Never commit it — tracked copies would go stale and cause merge conflicts.
dist/

.eslintcache
